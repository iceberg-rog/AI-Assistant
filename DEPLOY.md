# راه‌اندازیِ Shelter روی سرور (Deployment)

سیستمِ پشتیبانیِ هوشمندِ Shelter = یک **کانکتورِ تلگرام** (پیام‌ها رو می‌گیره، با Claude جواب می‌سازه) + یک **داشبوردِ وب** (روی :3939). هر دو داخلِ **یک کانتینرِ داکر** اجرا می‌شن، پس نصب فقط چند دستوره.

## ⚡ نصبِ سریع (کپی‌پیست روی یه سرورِ تازهٔ Ubuntu/Debian)
```bash
curl -fsSL https://get.docker.com | sh                                          # ۱. داکر (اگه نداری)
git clone https://github.com/iceberg-rog/AI-Assistant.git && cd AI-Assistant    # ۲. کد
cp .env.example .env && nano .env                                               # ۳. کلیدها رو پر کن
docker compose up -d --build                                                    # ۴. build + اجرا (~۳-۵ دقیقه)
```
بعد: مرورگر → `http://آی‌پیِ‌سرور:3939` → بات رو از تلگرام (Settings → Business → Chatbots) وصل کن.
توضیحِ هر مرحله + امنیت + رفعِ اشکال پایین ↓

---

## ۱) پیش‌نیازها
- یک سرورِ لینوکس (Ubuntu/Debian)، حداقل **۱ گیگ رم** و **۱ گیگ فضا**.
- نصبِ Docker و Docker Compose. اگه نداری:
  ```bash
  curl -fsSL https://get.docker.com | sh
  ```
  (پلاگینِ `docker compose` همراهِ نصبِ بالا میاد.)

## ۲) گرفتنِ کد
روی سرور:
```bash
git clone https://github.com/iceberg-rog/AI-Assistant.git
cd AI-Assistant
```
(اگه ریپو Private باشه، `git clone` با توکن یا SSH.)

## ۳) تنظیمِ کلیدها (`.env`)
از روی نمونه یک فایلِ `.env` بساز و پُرش کن:
```bash
cp .env.example .env
nano .env      # یا vim / vi
```
**حداقلِ لازم:**
| متغیر | چیه |
|---|---|
| `TELEGRAM_BOT_TOKEN` | از `@BotFather` (`/newbot`) |
| `CLAUDE_API_KEY` | کلیدِ Anthropic (مغزِ Claude) |
| `IP_REGISTRY_ALLIPS_URL` | endpointِ وایت‌لیستِ آیپی (فقط‌خواندنی) |
| `USERS_QUERY_BASE` | endpointِ DBِ کاربران (فقط‌خواندنی) |

مدل از قبل روی حالتِ اقتصادی-باکیفیت تنظیمه (`haiku → sonnet`). `SHELTER_AUTO_SEND=false` یعنی **Copilot** (هر جواب رو خودت تأیید می‌کنی) — اولش همین بمونه.

## ۴) اجرا
```bash
docker compose up -d --build
```
همین! کانتینر بالا میاد، داشبورد روی پورتِ **3939**، و کانکتور (با ری‌استارتِ خودکار) داخلش.

بررسیِ وضعیت:
```bash
docker compose ps
docker compose logs -f         # لاگِ زنده (Ctrl+C برای خروج)
```

## ۵) وصل‌کردنِ بات به اکانتِ پشتیبانی
بات خودش به اکانت وصل نمی‌شه — این کارو **داخلِ تلگرام** انجام می‌دی:
1. روی اکانتِ پشتیبانیت (که باید **Telegram Premium** داشته باشه) برو:
   **Settings → Telegram Business → Chatbots**
2. یوزرنیمِ باتت رو اضافه کن و مجوزِ **«Reply to messages»** رو بده.
3. تمام — تو داشبورد، صفحهٔ **اتصال تلگرام** اون اکانت رو زنده نشون می‌ده.

## ۶) بررسیِ سلامت
مرورگر رو باز کن: `http://آی‌پیِ‌سرور:3939`
- برو **اتصال تلگرام** → دکمهٔ **«تست اتصال»** → باید هر چهار اتصال سبز باشه (تلگرام، Claude، DB، وایت‌لیستِ آیپی).
- یه پیامِ تستی از یه اکانتِ دیگه به اکانتِ پشتیبانی بفرست → باید تو **مکالمات (زنده)** ظاهر شه.

---

## 🔒 امنیت (مهم — حتماً بخون)
داشبورد **رمز/احراز هویت نداره** و می‌تونه توکنِ بات رو عوض کنه و جواب بفرسته. اگه پورتِ 3939 رو روی اینترنت باز بذاری، **هر کسی** بهش دسترسی داره. یکی از این‌ها رو انجام بده:

- **فایروال (ساده‌ترین):** فقط آی‌پیِ خودت اجازه داشته باشه:
  ```bash
  sudo ufw allow from آی‌پیِ‌تو to any port 3939
  sudo ufw enable
  ```
- **فقط لوکال + تونلِ SSH:** تو `docker-compose.yml`، خطِ پورت رو بکن `"127.0.0.1:3939:3939"`، بعد از روی لپ‌تاپت:
  ```bash
  ssh -L 3939:localhost:3939 user@آی‌پیِ‌سرور
  ```
  و `http://localhost:3939` رو باز کن.
- **ریورس‌پروکسی با رمز:** پشتِ Nginx/Caddy با Basic-Auth + HTTPS.

---

## ۷) کار با سیستم
- **حالتِ ارسال:** پیش‌فرض Copilot (draft-only). وقتی به کیفیتِ درافت‌ها اعتماد کردی (مثلاً بعد از ۱۰۰–۳۰۰ پیامِ واقعی)، از صفحهٔ **تنظیمات** حالت رو روی «ارسالِ خودکار» بذار (یا `SHELTER_AUTO_SEND=true`).
- **عوض‌کردنِ بات/مدل:** از داشبورد (صفحهٔ اتصال) — توکن یا مدل رو عوض کن؛ کانکتور خودکار ری‌استارت می‌شه و تغییر تو `.env` ذخیره می‌مونه.
- **لاگ‌ها:**
  ```bash
  docker compose logs -f                          # کلِ کانتینر
  docker compose exec shelter tail -f dashboard/data/connector.log   # فقط کانکتور
  ```
- **به‌روزرسانی** (بعد از تغییرِ کد):
  ```bash
  git pull            # یا کدِ جدید رو بذار
  docker compose up -d --build
  ```
- **بکاپ:** کلِ وضعیتِ زنده (مکالمات، یادگیری، ارجاع‌ها) تو ولومِ `shelter-data`ـه:
  ```bash
  docker run --rm -v shelter_shelter-data:/d -v "$PWD":/b busybox tar czf /b/shelter-backup.tgz -C /d .
  ```
- **توقف / حذف:**
  ```bash
  docker compose down            # توقف (داده می‌مونه)
  docker compose down -v         # توقف + پاک‌کردنِ همهٔ دادهٔ زنده
  ```

## ۸) رفعِ اشکال
| علامت | راه‌حل |
|---|---|
| داشبورد باز نمی‌شه | `docker compose ps` + `docker compose logs`؛ پورتِ 3939 باز/فایروال درسته؟ |
| «تست اتصال» قرمز روی Claude | `CLAUDE_API_KEY` تو `.env` درسته؟ |
| «تست اتصال» قرمز روی DB/آیپی | `USERS_QUERY_BASE` / `IP_REGISTRY_ALLIPS_URL` درسته؟ |
| پیامِ کاربر نمیاد | بات روی اکانت با مجوزِ «Reply to messages» اضافه شده؟ صفحهٔ «اتصال تلگرام» اکانت رو نشون می‌ده؟ |
| بعد از ری‌استارت «۰ اکانت» | یک پیامِ جدید بفرست تا اتصال از روی ترافیک بازسازی شه، یا بات رو تو تنظیماتِ Businessِ اکانت یک‌بار بردار/دوباره اضافه کن |

---

## معماری (داخلِ کانتینر چی می‌گذره)
```
┌─ کانتینرِ shelter ─────────────────────────────┐
│  node core/run-live.ts   ← کانکتور (auto-restart)│
│        │ می‌نویسه ↓        ↑ control :4050        │
│  /app/dashboard/data  (ولوم: مکالمات، یادگیری…)  │
│        ↑ می‌خونه                                  │
│  next start :3939        ← داشبورد (نمایش + تأیید) │
└────────────────────────── پورتِ ۳۹۳۹ → بیرون ────┘
```
- KBِ ثابت (تاکسونومی، پاسخ‌های طلایی، …) داخلِ ایمیج baked شده و موقعِ استارت روی ولوم seed می‌شه.
- `.env` به‌صورتِ فایل mount می‌شه تا تغییراتِ داشبورد (توکن/مدل) ماندگار باشن.
- فقط پورتِ ۳۹۳۹ بیرونه؛ کنترل‌سرورِ ۴۰۵۰ فقط داخلِ کانتینره.
